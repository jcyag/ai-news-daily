# 环境变量配置示例
# 复制此文件为 .env 并填入你的实际值
# 注意：不要将 .env 文件提交到版本控制！

# ===================
# 邮件配置 (必填)
# ===================

# Gmail SMTP配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail账号
EMAIL_USER=your-email@gmail.com

# Gmail应用专用密码 (16位，非账户密码)
# 获取方式: Google账户 -> 安全 -> 两步验证 -> 应用专用密码
EMAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx

# 收件人邮箱 (多个用逗号分隔)
EMAIL_TO=your-email@gmail.com

# 发件人显示名称 (可选)
EMAIL_FROM=AI资讯日报 <your-email@gmail.com>

# ===================
# Reddit API配置 (可选)
# ===================
# 如果不配置，将跳过Reddit数据源

# Reddit API凭据
# 获取方式: https://www.reddit.com/prefs/apps
# 创建一个 "script" 类型的应用
REDDIT_CLIENT_ID=your-client-id
REDDIT_CLIENT_SECRET=your-client-secret
REDDIT_USER_AGENT=AI-News-Daily/1.0
